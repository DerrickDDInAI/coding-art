"""coding-art: computer-vision experiments that turn images into generative art.

Each sub-package is one self-contained project (mosaic, fractals, morphing, ...).
The code shared between them lives in :mod:`core.utils`.

Start with ``docs/cv-primer.md`` if you are new to computer vision.
"""
